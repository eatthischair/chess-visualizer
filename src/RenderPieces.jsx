import React, { useState, useRef, useEffect } from 'react';
import ReactDOM from 'react-dom';

const RenderPieces = ({pieceId, pieceElement}) => {
// console.log('props', pieceElement)

return pieceElement;
}

export default RenderPieces;